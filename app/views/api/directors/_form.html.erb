<%= form_with(model: director, local: true) do |form| %>
  <% if director.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(director.errors.count, "error") %> prohibited this director from being saved:
      </h2>

      <ul>
        <% director.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.file_field :upload, class: 'form-control-file' %>
  </div>

  <div class="form-group pt-3">
    <%= form.submit "submit", {class: "btn btn-primary"} %>
  </div>
<% end %>

<form>
  
</form>